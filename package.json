{
  "name": "Easycalc",
  "version": "2.1.5",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "publish": "electron-builder build --win --mac --linux -p always",
    "publish:win": "electron-builder build --win -p always",
    "publish:mac": "electron-builder build --mac -p always",
    "publish:linux": "electron-builder build --linux -p always",
    "build": "electron-builder build --win --mac --linux -p never",
    "build:win": "electron-builder build --win -p never",
    "build:mac": "electron-builder build --mac -p never",
    "build:linux": "electron-builder build --linux -p never"
  },
  "devDependencies": {
    "electron": "^7.1.2",
    "electron-builder": "^21.2.0"
  },
  "build": {
    "publish": [
      {
        "provider": "github",
        "owner": "tiois",
        "repo": "Easycalc",
        "releaseType": "release"
      }
    ],
    "directories": {
      "output": "dist"
    },
    "extraResources": [
      {
        "from": "./images/",
        "to": "images",
        "filter": [
          "**/*"
        ]
      },
      {
        "from": "./logo/",
        "to": "logo",
        "filter": [
          "**/*"
        ]
      }
    ]
  },
  "dependencies": {
    "build": "^0.1.4",
    "electron-log": "^3.0.9",
    "electron-updater": "^4.2.0",
    "jquery": "^2.2.4"
  }
}
